# Senha da tela de entrada do Cerne (feature/PasswordGate).
# Copie este arquivo para .env e mude o valor, ou defina a mesma variável
# em Vercel → Project Settings → Environment Variables.
# Sem isso definido, o app usa a senha padrão em src/features/auth/PasswordGate.tsx.
VITE_CERNE_PASSWORD=cerne123
